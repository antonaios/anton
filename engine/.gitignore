# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.coverage
htmlcov/

# Excel temporaries
~$*.xlsx
~$*.xlsm
~$*.xls

# Engine outputs (populated workbooks) — large, regenerable, never commit
runs/outputs/

# Audit log: keep the file but it's append-only and large; track decision per project.
# For now, keep it tracked so the run history is git-versioned. Revisit if it gets >10MB.

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.bak
